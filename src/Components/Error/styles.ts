import styled from 'styled-components'
import { cores } from '../../utils/styles'

export const ErrorStylized = styled.div`
  background-color: ${cores.creme};
  color: ${cores.vermelho};
  display: flex;
  justify-content: center;
  margin-top: 200px;
  margin-bottom: 200px;
`
